rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Auth 연동 후 members 배열 검증으로 교체
    // 현재 바이패스 모드: 모든 읽기/쓰기 허용
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
